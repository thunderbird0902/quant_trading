请审查 core/ 目录下的所有文件，做以下优化（不新增文件，在现有文件内改进）：

1. core/models.py：
   - 检查所有涉及价格、数量、金额的字段，是否使用了 Decimal 类型。如果用的是 float，请替换为 Decimal，并添加精度控制方法。
   - 确保所有 Model 类都有完整的 __repr__ 和数据校验（比如价格不能为负、数量不能为 0）。

2. core/event_bus.py：
   - 检查事件订阅/发布是否有异常隔离：一个订阅者的回调抛异常不应该影响其他订阅者的执行。如果没有，请添加 try-except 包裹每个回调调用，异常时记录日志但不中断事件传播。
   - 检查是否支持异步事件处理（async handler），如果当前是纯同步的，评估是否需要支持 async，但不要过度设计——如果现有使用场景都是同步的就保持同步。

3. core/engine.py：
   - 检查主引擎的启动和停止流程是否有优雅退出机制（signal handler 捕获 SIGINT/SIGTERM，先停策略 → 撤未完成订单 → 断开连接 → 保存状态）。如果没有，请添加。
   - 引擎启动时应该有一个自检流程：检查配置完整性、交易所连接、API 权限等，任一失败则拒绝启动并给出明确错误信息。

4. core/exceptions.py：
   - 检查异常类是否覆盖了关键场景：配置错误、网络错误、交易所 API 错误、风控拦截、数据异常。不够的补上，但不要过度细分——保持每类异常有明确的使用场景。

5. core/logger.py：
   - 日志是否支持结构化输出（JSON 格式）？如果只是普通文本日志，请添加一个可选的 JSON 格式化器，方便后续日志分析。
   - 检查是否有日志轮转配置（按天或按大小），没有的话请加上。
   - 交易相关日志需要包含：时间戳、策略名、交易对、方向、价格、数量、订单ID。检查现有日志调用是否满足，不满足的在后续模块优化时补上。

完成后列出你在每个文件中的具体改动，不新增文件。
