请审查 utils/ 和 tests/ 目录：

1. utils/retry.py：
   - 重试装饰器是否支持：最大重试次数、指数退避、可配置的异常类型过滤、重试时记录日志？缺失的补上。
   - 检查项目中关键位置（gateway API 调用、数据库操作）是否正确使用了重试。

2. utils/config_loader.py：
   - 配置加载是否有校验？必填字段缺失或类型错误时应给出明确错误信息，而不是运行到一半才崩溃。
   - API Key 等敏感信息是否支持从环境变量读取？应该是环境变量优先，fallback 到 YAML 文件。如果只支持 YAML，请添加环境变量优先逻辑。

3. utils/helpers.py：
   - 审查工具函数，删除未被项目任何地方 import 的废弃函数。

4. tests/：
   - 检查现有测试覆盖范围。不追求高覆盖率，但以下三个模块必须有测试，如果没有请补写（用 pytest）：
     
     a. risk/ 的风控规则 — 每条规则写触发和不触发两种 case：
        - order_validator：正常订单通过、价格偏离过大被拒、数量过小被拒
        - position_limit：未超限通过、超限被拒
        - loss_limit：未触发通过、日亏损超限后阻止开仓但允许平仓
     
     b. backtest/broker.py 的撮合逻辑：
        - 限价单在价格未触及时不成交、触及时成交
        - 市价单滑点计算正确
        - 手续费计算正确（maker/taker）
     
     c. core/models.py 的数据校验：
        - Decimal 精度正确
        - 非法值（负数价格、零数量）被拒绝

   这三个模块直接关系到资金安全，必须有测试覆盖。

不新增目录，测试文件放在现有的 tests/ 目录下。